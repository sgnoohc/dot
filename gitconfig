[alias]
	lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --
	st = status
	br = branch -a -v
[user]
	name = Philip Chang
	email = philip@ucsd.edu
	github = sgnoohc
[url "git@github.com:"]
	insteadOf = https://github.com/
